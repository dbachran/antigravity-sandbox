# Path to the Antigravity workspace on the host (mounted into the container)
WORKSPACE_DIR=/home/youruser/dev/antigravity

# Host user's local fonts directory (mounted read-only for UI rendering)
HOST_FONTS_DIR=/home/youruser/.local/share/fonts
